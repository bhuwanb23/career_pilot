# Mock Data Issue - Chat Not Calling Ollama

## Problem
Chat responses are coming from mock/canned workflow data, not from Ollama LLM. Backend logs don't show LLM calls.

## Evidence
- Cover letter response mentions "TechCorp" (mock company) instead of "Google" (user's request)
- No backend logs for LLM calls
- Responses look pre-written, not generated by AI

## Root Cause Investigation Needed
1. Check if workflows are returning canned responses instead of calling LLM
2. Check if LLM client has a fallback/mock mode
3. Check backend logs location

## Key Files
- services/llm_client.py - LLM provider
- services/workflows/cover_letter.py - workflow
- services/cover_letter.py - LLM call
- routers/chat.py - chat endpoint

# Phase 4.5 — Persona Generation

## Goal
Generate multiple career personas from parsed resume data. Each persona: match confidence, strengths, missing skills, suggested focus.

---

## Design: Persona System

Each persona is a "lens" over the same raw resume data — reweighting skills, reframing experience, and providing role-specific gap analysis.

---

## Step 1: Add CareerPersona Model

**File**: `backend/models.py`

```python
class CareerPersona(Base):
    __tablename__ = "career_persona"

    id = Column(Integer, primary_key=True, index=True)
    profile_id = Column(Integer, ForeignKey("career_profile.id"), nullable=False, index=True)
    persona_name = Column(String(100), nullable=False)
    persona_slug = Column(String(100), nullable=False)
    match_confidence = Column(Float, default=0.0)
    ai_summary = Column(Text, default="")
    highlighted_skills = Column(Text, default="[]")
    strengths = Column(Text, default="[]")
    missing_skills = Column(Text, default="[]")
    suggested_focus = Column(Text, default="[]")
    experience_level_label = Column(String(50), default="")
    target_role_types = Column(Text, default="[]")
    generated_at = Column(DateTime, nullable=True)
    created_at = Column(DateTime, default=utcnow)
    updated_at = Column(DateTime, default=utcnow, onupdate=utcnow)

    profile = relationship("CareerProfile", backref="personas")
```

---

## Step 2: Add Schemas

**File**: `backend/schemas.py`

```python
class PersonaItem(BaseModel):
    id: int
    persona_name: str
    persona_slug: str
    match_confidence: float
    ai_summary: str
    highlighted_skills: list[str]
    strengths: list[str]
    missing_skills: list[str]
    suggested_focus: list[str]
    experience_level_label: str
    target_role_types: list[str]
    generated_at: datetime | None = None

class PersonaGenerateRequest(BaseModel):
    persona_names: list[str] = ["Backend Engineer", "Frontend Engineer", "Full Stack Engineer", "AI/ML Engineer", "Data Engineer"]

class PersonaGenerateResponse(BaseModel):
    personas: list[PersonaItem]
    count: int
```

---

## Step 3: Create Persona Generator Service

**New file**: `backend/services/persona_generator.py`

LLM call that generates all personas at once:

```python
DEFAULT_PERSONAS = [
    "Backend Engineer", "Frontend Engineer", "Full Stack Engineer",
    "AI/ML Engineer", "Data Engineer"
]

async def generate_personas(profile_data: dict, persona_names: list[str] = None) -> list[dict]:
    names = persona_names or DEFAULT_PERSONAS
    prompt = f"""Analyze this career profile and generate career personas for each target direction.

Career Data:
{json.dumps(profile_data, indent=2)[:6000]}

Target personas: {', '.join(names)}

For EACH persona, return:
{{
  "persona_name": "...",
  "match_confidence": 0.0-1.0,
  "ai_summary": "persona-specific narrative",
  "highlighted_skills": ["skills emphasized for this direction"],
  "strengths": ["strengths framed for this role type"],
  "missing_skills": ["skills needed but not demonstrated"],
  "suggested_focus": ["actionable next steps"],
  "experience_level_label": "junior|mid|senior|lead",
  "target_role_types": ["specific job titles"]
}}

Return a JSON array of persona objects. Be specific and data-driven."""
```

---

## Step 4: Persona Service

**New file**: `backend/services/persona_service.py`

```python
def create_persona(db, profile_id, persona_data) -> CareerPersona
def get_personas(db, profile_id) -> list[CareerPersona]
def delete_persona(db, persona_id)
def persona_to_dict(persona) -> dict
```

---

## Step 5: Add Endpoints

**New file**: `backend/routers/personas.py`

```python
@router.post("/api/personas/generate")  # Generate all personas
@router.get("/api/personas")            # List personas
@router.get("/api/personas/{id}")       # Get one persona
@router.delete("/api/personas/{id}")    # Delete persona
```

---

## Step 6: Tests

**New file**: `backend/tests/test_personas.py`

---

## Files Summary

| Action | File |
|--------|------|
| Modify | `backend/models.py` |
| Modify | `backend/schemas.py` |
| Create | `backend/services/persona_generator.py` |
| Create | `backend/services/persona_service.py` |
| Create | `backend/routers/personas.py` |
| Modify | `backend/main.py` |
| Create | `backend/tests/test_personas.py` |

---

## Verification

1. **POST /api/personas/generate** → returns 5 personas with match confidence, strengths, missing skills
2. **GET /api/personas** → lists all personas
3. **GET /api/personas/{id}** → single persona detail
4. **Each persona** has unique summary, skills, strengths, gaps, and suggested focus
5. **Tests**: `pytest tests/test_personas.py` — all pass
